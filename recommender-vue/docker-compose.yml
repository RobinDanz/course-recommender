version: '3'

services:
  app:
    build:
    context: .
    dockerfile: Dockerfile
    container_name: recommender_vue
    ports:
      - 8080:80

  nginx:
    image: nginx:alpine
    container_name: nginx_recommender
    ports:
      - 80:80
    depends_on:
      - app
